// import { API_URL } from "@/config/_constant";

// Temp comment because can't get image
// export const mapImageLink = (src: string) => "/" + src;

export const mapImageLink = (src: string) => src || "/images/no-img.jpg";
